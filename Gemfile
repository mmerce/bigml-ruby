source "http://rubygems.org"

gemspec

# Will automatically pull in this gem and all its
# dependencies specified in the gemspec
gem "bigml", :path => File.expand_path("..", __FILE__)

group :development, :test do
  gem 'cucumber'
  gem 'test-unit'
end
